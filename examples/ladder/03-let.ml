// 03-let.ml
// Teaches: variable declaration, type annotation, identifier lookup.
// First program where the type checker has something to verify:
// the right-hand side of `=` must match the declared type.
// Expected output: 10

let x: int = 10;
print(x);
