// 04-bool.ml
// Teaches: the `bool` type and the literals `true` / `false`.
// Note: MiniLang prints bools as 1 / 0 because wasm has no bool type --
// both `int` and `bool` are represented as i32.
// Expected output: 1

let t: bool = true;
print(t);
